Heightmap renderer

To build:
make

To run:
./hw1 <heightmap>

Usage:
* 'q': render points.
* 'w': render wireframe.
* 'e': render mesh.

Extras:
* Uses element arrays to render lines and mesh.
* Custom shader.
